# Raw downloads, re-fetched every build. The parsed series in data/ is what is
# committed; these are just the wire format they arrived in.
egll_metar.csv
zbaa_metar.csv
__pycache__/
*.pyc
.DS_Store
tmp/

# `wrangler pages dev` scratch state, for previewing the site locally.
.wrangler/
